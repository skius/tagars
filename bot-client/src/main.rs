fn main() {
    // TODO: take in a CLI for server addr and amount of bots to spawn,
    // then for every bot spawn a thread that runs that bot.

    // TODO: check memory/CPU usage locally at like 1000 bots and find appropriate server.
    // also check how distributed the workload is across cores.
    println!("Hello, world!");
}
